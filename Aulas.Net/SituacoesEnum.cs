using System;

namespace Restaurante
{
    public enum SituacoesEnum
    {
        Novo = 1,
        Pago = 2,
        Cancelado = 3
    }
    public enum PagamentoEnum
    {
       Vista = (int)3.1,
       Prazo = (int)3.2
    }
}
